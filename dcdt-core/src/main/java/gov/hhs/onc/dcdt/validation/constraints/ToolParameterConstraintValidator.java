package gov.hhs.onc.dcdt.validation.constraints;

import java.lang.annotation.Annotation;

public interface ToolParameterConstraintValidator<T extends Annotation> extends ToolConstraintValidator<T, Object[]> {
}
